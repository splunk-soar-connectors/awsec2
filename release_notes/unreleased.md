**Unreleased**

* Escaped dynamic JavaScript values in EC2 action widgets.
* Bounded pagination and rejected repeated upstream page tokens.
* Applied security-group quarantine changes to every network interface using each interface's own groups.
